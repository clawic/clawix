<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clawix.App.Views.LoginGateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid HorizontalAlignment="Center" VerticalAlignment="Center" RowSpacing="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="0" Text="Welcome to Clawix" FontWeight="SemiBold" FontSize="28" />
    <TextBlock Grid.Row="1" Opacity="0.7" Text="Sign in with your Codex account to start." />
    <Button Grid.Row="2" Content="Sign in" Style="{ThemeResource AccentButtonStyle}" Click="SignIn_Click" />
    <HyperlinkButton Grid.Row="3" Content="Continue without signing in" Click="Skip_Click" />
  </Grid>
</UserControl>
