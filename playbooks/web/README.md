# Web Playbooks

Placeholder for future web visual playbooks.

When populated, use the same Markdown plus YAML front matter schema described in the root writing guide. Include browser, viewport, localhost, authentication, bridge pairing, and network interception expectations.
