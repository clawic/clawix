# Test Fixtures

Synthetic fixtures shared across lanes live here. Do not store real customer
data, private tokens, signing identities, Team IDs, bundle IDs, local machine
paths, paid API responses, or production exports.
