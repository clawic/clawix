import SwiftUI

/// Visual tokens for the Calendar mini-app. Populated incrementally as
/// each surface's spec sheet is approved; placeholder values used by the
/// Phase 0 scaffold until then.
enum CalendarTokens {
    enum Spacing {
        static let screenPadding: CGFloat = 16
    }

    enum Typography {
        static let headerSize: CGFloat = 22
        static let bodySize: CGFloat = 13
    }
}
