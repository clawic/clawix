# Linux Playbooks

Placeholder for future Linux desktop visual playbooks.

When populated, use the same Markdown plus YAML front matter schema described in the root writing guide. Include desktop environment, Tauri shell, daemon, package, and service-manager expectations.
