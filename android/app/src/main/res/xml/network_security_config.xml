<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- LAN private space -->
        <domain includeSubdomains="false">10.0.0.0</domain>
        <domain-suffix includeSubdomains="false">.local</domain-suffix>
        <!-- Tailscale CGNAT 100.64.0.0/10 - listing common ranges -->
    </domain-config>
    <!-- Permit cleartext for any IP. The bridge daemon listens on
         loopback/LAN/Tailscale IPs without TLS (Phase 2 has no local
         TLS yet). Pairing payload validates with bearer token. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
