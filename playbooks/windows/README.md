# Windows Playbooks

Placeholder for future Windows desktop visual playbooks.

When populated, use the same Markdown plus YAML front matter schema described in the root writing guide. Include WinUI shell, per-user daemon, filesystem, permission, and service lifecycle expectations.
